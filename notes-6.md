I think we need to adjust how adding shapes works. compare the solution in the current branch with the main branch on a deep level. Don't take shortcuts, i want you to know in depth how the adding of shapes works in both branches, how colors are applied to shapes, and how the shape is rendered on the canvas, e.g how in the current branch you can drag to resize it. I think the current solution, although it's an innovative way to solve the solution (whereby you can "stamp" shapes in one more and select them in the other), it frequently just gets in the way and makes it more difficult to add shapes. I think the old approach was better, where you just click on a shape button and it gets added to the middle of the canvas, selected. Then the user can move it around, rotate, resize etc. It just become more predictable than the current solution where you click on the shape button to select that shape, then click on the canvas to add it, and at that point if you click on the created shape you can resize, move, rotate, etc, but if you click anywhere else then you're "stamping" another shape on the canvas. And if you want to move several of the shapes around then you need to change to the "selecton" tool, which is just an extra step that gets in the way. I think we should just go back to the old solution, with a slight modification: instead of having a shape butotn for each combination of shape and color, we just have two shape buttons (like in the current branch), and the color that is currently selected in the color palette is the color that gets applied to the shape when you add it. And, still like the current implementation, when you change the color the shape button's shape changes color to reflect the currently selected color, so that it's clear to the user what color the shape will be when they add it. So the main difference from how it is now is that the shape gets added immediatley when you click the shape button, instead of having to click on the canvas to add it after selecting the shape button. Analyze the code and make sure that we're removing code that is no longer necessary after this change, such as the code that handles showing a "ghost shape" that follows the mouse around after you click the shape button. Another thing that this affects is the UI - the current language suggests that you select an option - select mode, or stamp shape A, or stamp shape B, i.e. they're radio buttons. But now we' remove the select button (as we're always in select mode, essentially), and the shape buttons are just buttons, not radio buttons, because you don't select them, you just click them to add the shape. So we need to change the design language/UI to reflect this change. Also, this means we can revert the keyboard shortcuts for mirroring from M and shift + M back to H and V for horizontal and vertical mirroring, respectively, because we no longer need a button (V) to switch to the select mode anymore. ![alt text](image-22.png)