(fyi desgin exploration repo is the workspace 2colors-ui-exploration)

- The pill button component should probably be renamed. Because we have a theme such as the "B" theme, where the overall style is a lot more square and sharp, where we have some elements that have absolutely no border radius, no round corners, but the pill button component has round corners. What I want to do is make it so that, when we swap themes to a theme that is more square in nature, such as the "B" theme, then I don't want the buttons to be rounded. I don't know if maybe the name of the component, which is pill button, has confused you, as it's name implies the shape, which is why I'm thinking we should rename it to just "Button"? Less confusing that way. The shape/design of the button depends on the style, it shouldn't be implied in the component name itself. Then I want you to make sure that, for the more rectangular hard themes, the buttons are more rectangular in nature (i think it's actually just the "b" theme).
- compare the "b" theme between the design exploration and the actual app: there are more distinct borders in the design exploration version (![alt text](image-19.png)) compared to the actual app (![alt text](image-20.png)), and that's kinda what sets it apart from the rest. Currently "b" and "c" are too similar, there's just a slight palette difference. e