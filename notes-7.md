- there's an issue with group behaviour. you're not able to take a few layers out of a group. there are only buttons to disband the group altogether. in the solution in the main branch there was an "ungroup" button which did just that. so we need to a) add that feature back and b) figure out how best to do this with the current design that doesn't have the overt "group" and "ungroup" buttons. Maybe a context aware button to ungroup? you can currently select some layers in the group and click the "group" button, which will bring them out of the first group and create a second group, and THEN you can click the "ungroup" button to disband it. it's a bit clunky but it works. maybe we can add an "ungroup" button that only appears when you have one of more layers in a group selected? ![alt text](image.png)